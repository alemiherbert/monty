#include "monty.h"

/**
 * main - this is an interpreter for monty bytecode
 * @argv: the argument vector
 * @argc: the number of arguments
 * Return: 0 if no errors occur
 */
int main()
{
    return (0);
}